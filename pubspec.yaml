name: flutter_test_developer
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-261.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  domain:
    path: ./domain

  core:
    path: ./core

  flutter_localizations:
    sdk: flutter

  intl: ^0.18.0

  use_cases:
    path: ./use_cases

  flutter_bloc: ^8.1.3
  auto_route: ^7.5.0
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  build_runner: ^2.4.3
  auto_route_generator: ^7.1.1

flutter:

  generate: true
  uses-material-design: true
